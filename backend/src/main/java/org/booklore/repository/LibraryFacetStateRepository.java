package org.booklore.repository;

import org.booklore.model.entity.LibraryFacetStateEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LibraryFacetStateRepository extends JpaRepository<LibraryFacetStateEntity, Long> {
}
