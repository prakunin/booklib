package org.booklore.repository;

import org.booklore.model.entity.CatalogStatStateEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CatalogStatStateRepository extends JpaRepository<CatalogStatStateEntity, Integer> {
}
