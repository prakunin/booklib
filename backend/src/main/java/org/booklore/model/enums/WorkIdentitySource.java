package org.booklore.model.enums;

/**
 * What established a work's identity. Recorded so a later run can tell a cheap, corroborated
 * resolution from one that cost an agent call and rests on nothing but the agent's word.
 */
public enum WorkIdentitySource {
    LOCAL,
    PROVIDER,
    AGENT
}
