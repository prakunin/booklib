import {describe, it} from 'vitest';

// TODO(seam): The metadata editor requires a high-fidelity form harness for Optimus UI
// upload, autocomplete, drag-drop author ordering, router navigation, and autosave flows.
describe('MetadataEditorComponent', () => {
  it.todo('needs an integration harness for form population, upload, save, and navigation branches');
});
