import {describe, it} from 'vitest';

// TODO(seam): This component mixes permission-driven effects, clipboard access, Optimus UI
// toast wiring, and service persistence, so it needs a mounted settings harness.
describe('HardcoverSettingsComponent', () => {
  it.todo('needs a mounted settings harness for permission hydration, save, and clipboard flows');
});
